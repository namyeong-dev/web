# macOS
.DS_Store

# 편집기 / 도구 설정
.claude/
.vscode/
.idea/

# 디자인 원본 (로고·목업·인스타그램 캡처) — 로컬에만 두고 저장소에는 올리지 않음.
# 사이트가 실제로 쓰는 이미지는 assets/img/ 에 복사되어 있음.
디자인/

# 스크린샷 검증용 하네스 — 로컬 확인용이라 저장소/배포에는 올리지 않음.
# (사이트 자체의 ?qa=1 모드는 js/main.js 에 있으므로 그대로 동작함)
qa.html

# 배포 스크립트 — 로컬에만 둠
scripts/
